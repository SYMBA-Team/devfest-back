import { Router } from "express";
//import { getStats, getStatsRevenue } from "../handlers/statics";

const router = Router();
/* router.get("/", getStats);
router.get("/revenue", getStatsRevenue); */

export default router;
